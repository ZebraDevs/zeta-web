import { css } from "lit";
export default css`
  .container {
    width: fit-content;
    display: flex;
    align-items: center;
    padding: var(--spacing-minimum) var(--spacing-small);
    background-color: var(--surface-warm);
    font: var(--body-x-small);
  }
`;
