import { Canvas } from '@storybook/blocks';
import { Tag } from './tag.stories';

# Tags
<Canvas of={Tag} />
Tags are sused to draw attention to a specific area or information.
The arrow shape helps direct the users attention to the desired place.