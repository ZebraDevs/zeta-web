import { Canvas } from "@storybook/blocks";

import {ButtonWithText} from "../stories/button.stories";
import {IconButton} from "../stories/icon-button.stories";

# Buttons
<br/><br/>
Buttons are buttons.
<Canvas of={ButtonWithText} />
Icon buttons are icon buttons.
<Canvas of={IconButton} />