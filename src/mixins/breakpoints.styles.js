import { css } from "lit";
export default css`
  /* Once Firefox 127 is released, we can convert this to CSS @property code. Maybe...?
--mobile-max-width: 1024px;

@property --sm {
  @media (max-width: #{$mobile-max-width}) {
    @content;
  }
}
*/
`;
